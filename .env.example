# Example environment variables
# Copy to .env and adjust values

# Database (PostgreSQL)
DATABASE_URL="postgresql://postgres:postgres@db:5432/pile?schema=public"

# Auth (NextAuth)
NEXTAUTH_SECRET="replace-with-strong-secret"
NEXTAUTH_URL="http://localhost:3000"

# Optional admin token for CMS writes
ADMIN_TOKEN="your-admin-token"

# (Add other service-specific envs here)
